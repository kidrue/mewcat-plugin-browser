# 2026-09-09 — 修复等价配置重复写入

**修改内容**：`src/state/translationService.ts` 使用深度相等比较替代 JSON 字符串比较，避免存储对象字段顺序变化触发无效修复写回。同步存储策略保持不变。

**原因**：一次读取和 130 次等价配置通知在修复前会产生 131 次写入，可能耗尽 Chrome 同步存储的写入配额；修复后该场景写入次数为 0。

**验证**：新增回归测试先失败后通过，配置测试 8 项通过，`pnpm format` 和 `pnpm check` 通过（10 条既有 lint warning）。浏览器现场仍需加载修复版本验证。

**记录位置**：用户已删除工作目录中的 `AGENTS.md`，本次保留删除状态，将修改记录单独保存于此。
